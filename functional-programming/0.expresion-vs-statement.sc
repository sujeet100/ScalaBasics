//expresion-is something that yields a value
val x = 10
val y = 15
x + y

val result = if (x == 10)
  "Ten"
else "15"

//No side effects
//An expression is said to have a side effect if it modifies some state variable value(s) outside its local environment.

//statement-is something that does some action
//may-contain side-effects

println("Hello World")


//functional programming encourages use of expression

