//simple-function
def add(num1: Int, num2: Int): Int = {
  num1 + num2
}

add(1, 2)

//function-literal
(num1: Int, num2: Int) => num1 + num2

res1(1, 2)
//We just right entire function implementation instead of passing the reference of the function.